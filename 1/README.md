
## 1

If you haven't already, you need to set up `CMSIS`. Go to the root folder of this repository and follow the instructions in `setup_cmsis.sh` (or just make it executable and run it).

Build the software here by `make`, and flash by `make flash`.